# CannaScope CT — V2  (this repo's current version)
# Core:
requests==2.34.2
reportlab==4.5.1
pypdfium2==5.8.0
Pillow==12.2.0

# OCR — lets the tool read scanned/image-only COAs (recommended).
#   macOS uses Apple Vision via ocrmac (no extra system install needed).
#   Other systems use tesseract (also `apt install tesseract-ocr` etc.).
ocrmac==1.0.1 ; sys_platform == 'darwin'
pytesseract==0.3.13 ; sys_platform != 'darwin'

# ---------------------------------------------------------------------------
# The older Beta V1 script (connecticut_cannabis_contaminant_checker_beta_v1.py)
# instead needs:  pip install requests pdfplumber reportlab
# (V1 remains available as a tagged release for anyone who wants it.)
